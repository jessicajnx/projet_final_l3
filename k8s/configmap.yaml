apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: webapp
data:
  POSTGRES_DB: appdb
  POSTGRES_USER: appuser
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: appdb
  DB_USER: appuser
  API_MESSAGE: "Hello from Kubernetes"
